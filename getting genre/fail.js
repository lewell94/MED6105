//Spotify - Not all artists have genres, also how to access?

/*function artistSearch() {
	$.get('https://api.spotify.com/v1/search?q=' + searchValue + '&type=artist', function(data) {
        console.log(data)
        $(data.artists.items).each(function(){
            $('body').append(this.streamable);
        });
    })
};*/

//iTunes - How to get it to work

/*function artistSearch() {
	$.get('https://itunes.apple.com/search?term=jack+johnson&entity=musicArtist', function(data) {
        console.log(data)
        $(data.artist).each(function(){
            $('body').append(this.streamable);
        });
    })
};*/